# Magnus集成到vLLM的依赖项

# 核心依赖
vllm>=0.6.0
torch>=1.12.0
transformers>=4.20.0
numpy>=1.21.0

# 机器学习组件（可选，用于更好的预测效果）
scikit-learn>=1.0.0

# 数据处理
pandas>=1.3.0

# 可选的语义特征提取（如果需要LaBSE）
sentence-transformers>=2.0.0

# 开发和测试
pytest>=6.0.0
pytest-asyncio>=0.18.0

# 监控和可视化（可选）
matplotlib>=3.5.0
seaborn>=0.11.0

# 配置管理
pyyaml>=6.0
